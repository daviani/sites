{
  "name": "sites",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio multi-tenant avec Turborepo + Next.js 14",
  "author": "Daviani Fillatre",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules .turbo"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0",
    "turbo": "^2.1.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@10.22.0",
  "pnpm": {
    "overrides": {
      "glob": ">=10.5.0",
      "js-yaml": ">=3.14.2"
    }
  },
  "dependencies": {
    "puppeteer": "^24.34.0"
  }
}
